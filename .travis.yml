language: php

php:
  - 5.4

before_script:
  - flags=""
  - composer install
#  - app/yiic environment travis
  - php -S localhost:31415 -t web/ &
  - git config --global user.name aberti
  - git config --global user.email travis@example.com

script: vendor/bin/codecept run

branches:
  only:
    - master